let idades = [12, 18, 25, 10, 30, 15];
// utilizando a função filter para filtrar as idades >= 18 e retornar somente os valores que forem iguals a condição
let maiorIdade = idades.filter(idades => idades >= 18)
console.log(idade)