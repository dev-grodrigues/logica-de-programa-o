let numbers =  [1, 2, 3, 4, 5, 6];
// Definindo o metodo filter para mostrar na variavel numPar, para mostrar somente os numeros pares!
let numPar = numbers.filter(numbers => numbers % 2 == 0);
console.log(numPar);